import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700", "800", "900"],
});

export const metadata: Metadata = {
  title: "Darren's Health Dashboard",
  description:
    "Personal health data dashboard. Labs, fitness, sleep, and recovery — all in one place with AI-powered insights.",
  openGraph: {
    title: "Darren's Health Dashboard",
    description:
      "Personal health data visualized. Sleep, HRV, labs, fitness, and AI insights.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} h-full antialiased`}>
      <body className="min-h-full">{children}</body>
    </html>
  );
}
