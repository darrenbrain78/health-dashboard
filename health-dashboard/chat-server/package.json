{
  "name": "health-chat-server",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "dev": "tsx index.ts",
    "build": "npx tsc index.ts --outDir . --esModuleInterop --resolveJsonModule --moduleResolution node --target ES2020 --module commonjs --skipLibCheck"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.52.0",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  }
}
